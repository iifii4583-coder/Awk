{
  "name": "Debian Trixie KDE Full Desktop",
  "image": "debian:trixie",
  "remoteUser": "root",
  "postCreateCommand": "apt update && apt upgrade -y && apt install -y sudo bash curl wget git vim nano net-tools software-properties-common kde-standard kde-plasma-desktop sddm breeze-icon-theme papirus-icon-theme firefox-esr tigervnc-standalone-server python3 python3-pip && git clone https://github.com/novnc/noVNC.git /opt/noVNC && git clone https://github.com/novnc/websockify /opt/noVNC/utils/websockify && mkdir -p /root/.vnc && echo 'password' | vncpasswd -f > /root/.vnc/passwd && chmod 600 /root/.vnc/passwd && vncserver :1 -geometry 1024x600 -depth 24 && cd /opt/noVNC && ./utils/novnc_proxy --vnc localhost:5901 --listen 6081 &",
  "forwardPorts": [6081],
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-vscode.cpptools",
        "eamodio.gitlens"
      ]
    }
  }
}
